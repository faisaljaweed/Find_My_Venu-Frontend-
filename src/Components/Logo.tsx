import { NavLink } from "react-router-dom";

const Logo = () => {
  return (
    <div className="text-3xl font-bold">
      <h2>
        <NavLink to="/">Find By Venu</NavLink>
      </h2>
    </div>
  );
};

export default Logo;
